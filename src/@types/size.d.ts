export interface IWindowSizeProps {
  windowSize: { width: number; height: number }
  isMobile: boolean
  isTablet: boolean
  isDesktop: boolean
}

export interface ISize {
  width: number
  height: number
}
